# =============================================================================
# 位置：deploy/docker/.env.example
# 用法：复制为本目录下的 .env（与 docker-compose.yml 同级）后填写真实值：
#       cp .env.example .env
# 勿将填好密码的 .env 提交到 Git。
# 极空间也可在 Docker 界面为 backend 容器手动添加同名变量（与 .env 二选一）。
# =============================================================================

PORT=3001
NODE_ENV=production

# 必须：生产环境强随机串；含 # 时请用双引号包起来，例如 JWT_SECRET="xxx#yyy"
JWT_SECRET=请替换为长随机字符串
JWT_EXPIRES_IN=7d

# ---------- MySQL（使用「已有」库：其它 Docker 容器或宿主机）----------
# 任选 A / B / C 一种，不要混填多套 MYSQL_HOST。

# —— A（推荐）：MySQL 容器已映射宿主机端口，如 -p 3306:3306 ——
MYSQL_HOST=host.docker.internal
MYSQL_PORT=3306
# 若映射为 3307:3306，则改为 MYSQL_PORT=3307

# —— B：与 backend 在同一 Docker 自定义网络时，用 MySQL 容器名 ——
# MYSQL_HOST=你的mysql容器名
# MYSQL_PORT=3306

# —— C：库在局域网其它机器 ——
# MYSQL_HOST=192.168.0.1
# MYSQL_PORT=3306

MYSQL_USER=你的用户
MYSQL_PASSWORD=你的密码
MYSQL_DATABASE=lexing_school

UPLOAD_DIR=/app/uploads

# 模板文件存储目录（须与 compose 中 templates 卷挂载路径一致，默认 /app/templates）
# 若库中 file_path 仍是旧环境绝对路径，或容器重建后文件丢失，预览/下载会 404，需重新上传模板或修正文件
TEMPLATE_STORAGE_PATH=/app/templates

# Excel HTML 预览：磁盘缓存在 /app/templates/cache/；内存条数上限与定期清理、并发单飞见 templatePreviewCache.js
# PREVIEW_HTML_MEMORY_TTL_MS=900000
# PREVIEW_HTML_MEMORY_MAX_ENTRIES=200
# PREVIEW_HTML_MEMORY_SWEEP_MS=300000

# 浏览器访问前端的完整来源（协议+域名+端口）。动静分离时多个域名用英文逗号分隔，勿加空格或改用引号包裹整行。
# 内网示例：http://192.168.1.100:8888
# 公网单域名：https://admin.example.com
# 官网 www + 管理端 admin 示例：
# CORS_ORIGIN=https://www.lexingronghe.com,https://admin.lexingronghe.com
CORS_ORIGIN=http://192.168.1.100:8888
